import { Rubik, Inter, DM_Sans } from 'next/font/google'

// export const useRubik = Rubik({ subsets: ['latin'] });
export const useRubik = DM_Sans({ subsets: ['latin'], weight: ['400', '500', '700'] });
export const useInter = Inter({ subsets: ['latin'] });
