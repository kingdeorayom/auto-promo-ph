import { useRubik } from '../utils/fonts';

const Header = () => {
    return (
        <header className={`${useRubik.className}`}>
            <nav>Header and Nav</nav>
        </header>
    )
}

export default Header