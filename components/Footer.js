import { useRubik } from "@/utils/fonts"

const Footer = () => {
    return (
        <footer className={`${useRubik.className}`}>
            <h4>Footer</h4>
        </footer>
    )
}

export default Footer